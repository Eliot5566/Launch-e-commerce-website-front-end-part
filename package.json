{
  "name": "japansnack-main",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd frontend && npm install",
    "start": "cd frontend && npm start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "render-postbuild": "cd frontend && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.0.3",
    "prettier-eslint": "^15.0.1"
  }
}
